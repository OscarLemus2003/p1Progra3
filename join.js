// Array of strings
const fruits = ['Apple', 'Banana', 'Orange', 'Mango'];

// Using join to concatenate array elements into a string separated by commas
const joinedString = fruits.join(', ');

// Output the result
console.log(joinedString);
