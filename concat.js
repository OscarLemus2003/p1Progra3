// Crear dos arreglos
const arreglo1 = [1, 2, 3];
const arreglo2 = [4, 5, 6];

// Usar el método concat para combinar los arreglos
const arregloConcatenado = arreglo1.concat(arreglo2);

// Mostrar el resultado
console.log(arregloConcatenado);
